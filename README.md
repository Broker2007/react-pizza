# 🍕 React Pizza

Современное веб-приложение для заказа пиццы, разработанное с использованием React и TypeScript. Проект демонстрирует работу с Redux Toolkit для управления состоянием, интеграцию с REST API и полноценную корзину покупок.

> **Проект разработан в 2023 году**

## 📋 Оглавление

- [Описание](#описание)
- [Демонстрация работы сайта](#демонстрация-работы-сайта)
- [Технологии](#технологии)
- [Функциональность](#функциональность)
- [Структура проекта](#структура-проекта)
- [Установка и запуск](#установка-и-запуск)
- [Скриншоты](#скриншоты)
- [Особенности реализации](#особенности-реализации)

## 📝 Описание

React Pizza — это полнофункциональное приложение для заказа пиццы с интуитивно понятным интерфейсом. Проект включает в себя клиентскую часть на React и серверную часть на Node.js с использованием MongoDB для хранения данных.

### Основные возможности:

- 📱 Адаптивный дизайн
- 🔍 Поиск пицц по названию
- 🎯 Фильтрация по категориям
- 📊 Сортировка по популярности, цене и алфавиту
- 🛒 Полнофункциональная корзина
- 💾 Сохранение данных в MongoDB
- ⚡ Оптимизированная производительность

## 🎬 Демонстрация работы сайта

![Демонстрация проекта](./demo/bandicam%202023-10-01%2013-12-37-594.gif)

📹 [Скачать оригинальное видео](./demo/bandicam%202023-10-01%2013-12-37-594.mp4)

## 🛠 Технологии

### Frontend:

- **React 18.2.0** — библиотека для построения пользовательского интерфейса
- **TypeScript 5.1.6** — типизированный JavaScript
- **Redux Toolkit 1.9.5** — управление состоянием приложения
- **React Router DOM 6.11.1** — маршрутизация
- **Axios 1.4.0** — HTTP-клиент для работы с API
- **SCSS/SASS** — препроцессор CSS для стилизации
- **React Content Loader** — скелетоны загрузки

### Backend:

- **Node.js** — серверная платформа
- **Express.js 4.18.2** — веб-фреймворк
- **MongoDB 7.0.2** (Mongoose) — база данных
- **CORS** — обработка кросс-доменных запросов

### Инструменты разработки:

- **Create React App** — сборка проекта
- **Prettier** — форматирование кода
- **TypeScript** — статическая типизация

## ✨ Функциональность

### Главная страница

- Отображение каталога пицц с изображениями, названиями и ценами
- Фильтрация по категориям: Все, Мясные, Вегетарианская, Гриль, Острые, Закрытые
- Сортировка по:
  - Популярности (рейтинг)
  - Цене
  - Алфавиту
- Поиск пицц по названию в реальном времени
- Выбор типа теста (тонкое/традиционное) и размера пиццы
- Добавление пицц в корзину с выбранными параметрами

### Страница корзины

- Просмотр всех добавленных пицц
- Отображение общей суммы заказа
- Подсчет количества товаров
- Удаление отдельных позиций
- Очистка всей корзины
- Кнопка возврата на главную страницу

### Дополнительные функции

- Skeleton-экраны во время загрузки данных
- Обработка ошибок при работе с API
- Асинхронные запросы с использованием Redux Thunk
- Сохранение состояния корзины в базе данных

## 📁 Структура проекта

```
react-pizza/
├── public/                 # Публичные файлы
│   ├── index.html
│   ├── manifest.json
│   └── robots.txt
├── src/
│   ├── components/         # React компоненты
│   │   ├── CartBlock/     # Компоненты корзины
│   │   │   └── PizzaCart.tsx
│   │   ├── Input/         # Компонент поиска
│   │   │   ├── index.tsx
│   │   │   └── Input.module.scss
│   │   ├── NotFoundBlock/  # Страница 404
│   │   ├── pages/         # Страницы приложения
│   │   │   ├── Home.tsx
│   │   │   ├── Cart.tsx
│   │   │   └── NotFound.tsx
│   │   ├── PizzaBlock/    # Карточка пиццы
│   │   │   ├── index.tsx
│   │   │   └── Skeleton.tsx
│   │   ├── Categories.tsx # Фильтр категорий
│   │   ├── Sorting.tsx    # Компонент сортировки
│   │   └── Header.tsx     # Шапка сайта
│   ├── redux/             # Redux хранилище
│   │   ├── Slices/
│   │   │   ├── CartSlice.ts    # Управление корзиной
│   │   │   ├── FilterSlice.ts  # Фильтры и поиск
│   │   │   └── PizzaSlice.ts   # Данные пицц
│   │   └── store.ts       # Конфигурация store
│   ├── scss/              # Стили приложения
│   │   ├── app.scss
│   │   ├── _variables.scss
│   │   ├── _fonts.scss
│   │   ├── components/    # Стили компонентов
│   │   └── libs/         # Библиотеки стилей
│   ├── assets/           # Изображения и иконки
│   ├── utils/            # Вспомогательные функции
│   │   └── cartPriceRender.ts
│   ├── App.tsx           # Главный компонент
│   └── index.tsx         # Точка входа
├── server/               # Серверная часть
│   ├── index.js          # Express сервер
│   └── package.json
├── build/                # Собранная версия
├── package.json
└── tsconfig.json

```

## 🚀 Установка и запуск

### Предварительные требования

- Node.js (версия 14 или выше)
- MongoDB (локально установленная или удаленная)
- npm или yarn

### Установка зависимостей

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd REACT-PIZZA
```

2. Установите зависимости для клиентской части:

```bash
cd react-pizza
npm install
```

3. Установите зависимости для серверной части:

```bash
cd ../server
npm install
```

### Настройка базы данных

Убедитесь, что MongoDB запущена локально на порту 27017, или измените строку подключения в `server/index.js`:

```javascript
mongoose.connect("mongodb://127.0.0.1:27017/react-pizza");
```

### Запуск приложения

1. Запустите сервер (в папке `server`):

```bash
npm start
```

Сервер будет доступен на `http://localhost:3000`

2. В новом терминале запустите клиентскую часть (в папке `react-pizza`):

```bash
npm start
```

Приложение откроется в браузере на `http://localhost:3000` (или следующем доступном порту)

### Сборка для продакшена

```bash
cd react-pizza
npm run build
```

Собранные файлы будут находиться в папке `build/`.

## 🎨 Особенности реализации

### Redux Toolkit

Проект использует современный подход к управлению состоянием через Redux Toolkit:

- **CartSlice** — управление корзиной покупок с асинхронными действиями для работы с API
- **FilterSlice** — состояние фильтров, сортировки и поиска
- **PizzaSlice** — загрузка и кэширование данных о пиццах

### Асинхронные операции

Использование `createAsyncThunk` для обработки асинхронных запросов:

- Загрузка списка пицц с фильтрацией
- Добавление/удаление товаров из корзины
- Обработка состояний загрузки и ошибок

### Типизация TypeScript

Полная типизация всех компонентов, пропсов и состояний для повышения надежности кода и улучшения опыта разработки.

### Модульная архитектура

- Разделение компонентов по функциональности
- Переиспользуемые компоненты
- Централизованное управление стилями через SCSS

### API Endpoints

Серверная часть предоставляет следующие эндпоинты:

- `POST /pizzas` — получение списка пицц с фильтрацией и сортировкой
- `GET /cart` — получение содержимого корзины
- `POST /cart/add` — добавление пиццы в корзину
- `GET /cart/remove` — удаление пиццы из корзины
- `GET /cart/removeAll` — очистка корзины


## 🔧 Возможные улучшения

- [ ] Добавление аутентификации пользователей
- [ ] Интеграция платежной системы
- [ ] История заказов
- [ ] Избранные пиццы
- [ ] Адаптивный дизайн для мобильных устройств
- [ ] Оптимизация производительности (React.memo, useMemo)
- [ ] Unit-тесты (Jest, React Testing Library)
- [ ] E2E тесты (Cypress)

## 📄 Лицензия

Этот проект создан в образовательных целях для портфолио.

## 👨‍💻 Автор

Проект разработан в 2023 году как демонстрация навыков работы с современным стеком веб-разработки.

---

**Примечание**: Для полноценной работы приложения необходимо запустить серверную часть и убедиться, что MongoDB запущена и доступна.
